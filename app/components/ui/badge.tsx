export function Badge({className='', children}:{className?:string; children:any}){return <span className={`inline-flex items-center px-2 py-1 text-xs font-medium rounded ${className}`}>{children}</span>}